[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "jarvis_db"
version = "0.0.11"
dependencies = [
    "SQLAlchemy",
    "jorm @ git+https://github.com/PickAim/jorm.git@setup",
]
